name: First Workflow

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - main

jobs:
  first:
    name: The first job in the first workflow
    runs-on: ubuntu-latest
    steps:
      - run: exit 1